# Research Specialist Agent
# Example agent definition file

name: "researcher"
agent_type: "tool_calling"
role: |
  Research specialist. Search, fetch, summarize. Be concise.

tools:
  - tavily_web_search

model:
  provider: "openai"
  model: "gpt-4o-mini"
  temperature: 0.7
  max_tokens: 5000

settings:
  verbosity: 5
  max_steps: 40

metadata:
  description: "Ddicated Column Matcher Agent"
  author: "system"
  version: "1.0.1"
